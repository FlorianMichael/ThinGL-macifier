publishing {
    repositories {
        maven {
            name = "reposilite"
            url = "https://maven.florianmichael.de/" + (project.maven_version.endsWith("SNAPSHOT") ? "snapshots" : "releases") // FlorianMichael - Change repository server

            credentials(PasswordCredentials)
            authentication {
                basic(BasicAuthentication)
            }
        }
    }
}
